# PipeScan 管道机器人项目说明

本项目包含两个部分：

1. 工程师控制面板：用于连接机器人、发送运动控制指令、配置摄像头、录入检测缺陷。
2. 后端评估与报告：根据机器人或人工录入的缺陷数据计算风险等级，并生成 Markdown 检测报告。

## 目录结构

```text
pipescan/
├─ backend/
│  ├─ main.py              # FastAPI 后端服务入口
│  ├─ mian.py              # 兼容原有拼写的入口文件，导出 main.py 中的 app
│  ├─ evaluator.py         # 管道缺陷评估算法
│  ├─ reportgenerator.py   # Markdown 报告生成器
│  ├─ requirement.txt      # Python 依赖
│  └─ reports/             # 运行后自动生成，保存检测报告
├─ frontend/
│  ├─ package.json         # Vite/Vue 依赖和启动脚本
│  ├─ index.html           # 前端 HTML 入口
│  └─ src/
│     ├─ main.js           # Vue 应用挂载入口
│     ├─ App.vue           # 控制面板主页面
│     └─ style.css         # 控制面板样式
└─ 项目说明.md
```

## 后端代码作用

### `backend/main.py`

这是后端 API 服务入口，使用 FastAPI 实现。

主要接口：

- `GET /api/health`：健康检查。
- `POST /api/robot/connect`：模拟连接机器人。
- `GET /api/robot/status`：读取机器人状态。
- `POST /api/robot/motion`：发送运动控制指令，支持 `forward`、`backward`、`left`、`right`、`stop`、`set_speed`。
- `GET /api/camera`：读取摄像头配置。
- `POST /api/camera`：设置摄像头源、分辨率、启用状态。
- `GET /api/camera/snapshot`：获取当前帧信息占位接口。
- `POST /api/inspection/evaluate`：根据缺陷数据计算 F、G、RI、MI 和等级。
- `POST /api/inspection/report`：计算评估结果并生成 Markdown 报告。

其中 `MotionController` 是运动控制适配器，后续可以在 `send_command` 中接入串口、CAN、TCP、ROS 或嵌入式控制板 SDK。

`CameraAdapter` 是摄像头适配器，后续可以替换为 USB 摄像头、RTSP 视频流或厂商 SDK。

### `backend/evaluator.py`

这是检测算法模块，核心类是 `PipeDefectEvaluator`。

它接收以下数据：

- 管段长度
- 管径
- 区域重要性
- 土质类型
- 缺陷列表

输出以下指标：

- `F_structural`：结构性缺陷参数
- `G_functional`：功能性缺陷参数
- `RI_repair_index`：修复指数
- `MI_maintenance_index`：养护指数
- 结构等级、功能等级、修复等级、养护等级

缺陷数据使用 `Defect` 数据类表示，包含缺陷类型、缺陷代码、分值、长度、距离和描述。

### `backend/reportgenerator.py`

这是报告生成模块，核心类是 `ReportGenerator`。

主要能力：

- `generate_markdown(evaluation)`：把评估结果转换为 Markdown 文本。
- `save_markdown(evaluation, output_dir)`：把报告保存到指定目录。

生成的报告包含：

- 管段基本信息
- 评估结论
- 核心指标表格
- 缺陷明细表格
- 工程处理建议

### `backend/mian.py`

原项目中存在 `mian.py` 文件名。为了避免旧启动命令失效，这个文件保留为兼容入口，内部直接导出 `main.py` 里的 `app`。

## 前端代码作用

### `frontend/src/App.vue`

这是工程师控制面板主页面，包含四个主要区域：

- 设备状态：显示连接状态、模式、电量、里程和速度。
- 运动控制：提供前进、后退、左转、右转、停止按钮。
- 摄像头接口：设置视频源、分辨率和启用状态。
- 检测数据：录入管段信息和缺陷明细，调用后端生成评估结果和报告。

前端默认请求 `http://localhost:8000`。如果后端部署在其他地址，可以在前端环境变量中设置：

```bash
VITE_API_BASE=http://你的后端地址:端口
```

### `frontend/src/style.css`

控制面板样式文件，负责布局、表格、按钮、视频占位区域和响应式适配。

### `frontend/src/main.js`

Vue 应用入口，负责加载样式并挂载 `App.vue`。

## 启动方式

### 启动后端

```bash
cd backend
pip install -r requirement.txt
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

### 启动前端

```bash
cd frontend
npm install
npm run dev
```

启动后访问 Vite 输出的本地地址，通常是：

```text
http://localhost:5317
```

## 后续接入硬件的位置

### 运动控制板

修改 `backend/main.py` 中的 `MotionController.send_command`。

建议把嵌入式协议封装成独立模块，例如：

```text
backend/hardware/motion_serial.py
backend/hardware/motion_can.py
backend/hardware/motion_tcp.py
```

然后由 `MotionController` 调用这些模块。

### 摄像头

修改 `backend/main.py` 中的 `CameraAdapter`。

常见接入方式：

- USB 摄像头：OpenCV `cv2.VideoCapture(0)`
- RTSP 摄像头：OpenCV 或 FFmpeg 读取 RTSP 地址
- 厂商 SDK：在 `CameraAdapter.snapshot` 或新增视频流接口中调用 SDK

### 算法扩展

修改 `backend/evaluator.py`。

可以继续增加：

- 缺陷图片置信度
- 机器人姿态和里程误差修正
- 多帧缺陷合并
- 不同管材、管龄、管径的权重修正
- 数据库历史记录对比

## 当前状态

当前版本是一个可运行的工程骨架。运动控制和摄像头接口是模拟适配器，接口形式已经预留，适合下一步接入真实嵌入式控制板和真实视频流。
