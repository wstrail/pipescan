# PipeScan 项目启动指南

```1:docker/postgres数据库启动
docker compose up -d postgres
2：启动后端
cd backend
uvicorn main:app --reload --host 0.0.0.0 --port 8000
3：启动前端
cd frontend
npm run dev
```

本文档用于从零启动 PipeScan 项目，包括 PostgreSQL 数据库、FastAPI 后端和 Vue/Vite 前端。

## 1. 项目目录

```text
D:\PythonProjects\pipescan
├─ backend      后端 FastAPI 服务
├─ frontend     前端 Vue/Vite 控制台
├─ .venv        Python 虚拟环境
└─ docker-compose.yml
```

## 2. 启动顺序

必须按这个顺序启动：

```text
PostgreSQL 数据库 -> FastAPI 后端 -> Vue 前端
```

## 3. 启动 PostgreSQL 数据库

进入项目根目录：

```powershell
cd D:\PythonProjects\pipescan
```

启动数据库：

```powershell
docker compose up -d postgres
```

查看数据库容器状态：

```powershell
docker compose ps
```

正常情况下会看到 `pipescan-postgres` 处于 `running` 或 `healthy` 状态。

数据库配置：

```text
数据库：pipescan
用户：pipescan
密码：pipescan
端口：5432
```

后端连接配置在：

```text
backend/.env
```

示例：

```env
DATABASE_URL=postgresql+psycopg://pipescan:pipescan@localhost:5432/pipescan
```

## 4. 初始化数据库表

后端启动时会自动创建表。当前主要表包括：

```text
pipe_segments        管段基础信息
inspection_records   用户填写的检测数据
inspection_reports   生成后的报告
```

如果你想手动初始化，可以执行：

```powershell
cd D:\PythonProjects\pipescan\backend
..\.venv\Scripts\python.exe -c "from database import init_db; print(init_db())"
```

看到下面结果说明成功：

```text
(True, 'database ready')
```

## 5. 启动后端

打开一个 PowerShell，执行：

```powershell
cd D:\PythonProjects\pipescan
.\.venv\Scripts\Activate.ps1
cd backend
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

后端启动成功后访问：

```text
http://localhost:8000
```

接口文档：

```text
http://localhost:8000/docs
```

数据库连接检查：

```text
http://localhost:8000/api/db/health
```

正常返回：

```json
{
  "connected": true,
  "message": "database connected"
}
```

## 6. 启动前端

再打开一个新的 PowerShell，执行：

```powershell
cd D:\PythonProjects\pipescan\frontend
npm run dev
```

前端固定端口：

```text
http://localhost:5317
```

## 7. 推荐启动命令汇总

第一个终端：

```powershell
cd D:\PythonProjects\pipescan
docker compose up -d postgres
```

第二个终端：

```powershell
cd D:\PythonProjects\pipescan
.\.venv\Scripts\Activate.ps1
cd backend
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

第三个终端：

```powershell
cd D:\PythonProjects\pipescan\frontend
npm run dev
```

然后打开：

```text
http://localhost:5317
```

## 8. 模拟传感器系统

当前项目提供一个明确标记为 `SIMULATION-ONLY` 的嵌入式机器人模拟脚本，用来在没有真实机器人时测试。它和最终真实硬件接入使用同一套框架和接口：

```text
前端控制台 -> 后端运动接口 -> 嵌入式/模拟器读取命令
嵌入式/模拟器 -> 后端状态接口 -> 前端显示速度、里程、报警
嵌入式/模拟器 -> 后端报告接口 -> 自动写入真实检测缺陷
```

它不是前端自己造数据，而是模拟机器人传感器在运动过程中发现问题后回传平台：

- 机器人路径、速度、里程、电量、姿态等状态变化。
- 前方障碍或严重管道破损时的控制面板报警。
- 只有遇到预设管道问题点时，才写入 `pipe_segments`、`inspection_records`、`inspection_reports`。

运行嵌入式模拟器：

```powershell
cd D:\PythonProjects\pipescan
.\.venv\Scripts\python.exe scripts\simulate_robot_sensor.py
```

运行后，在前端按正常方式操作：

```text
新建巡检 -> 设置速度 -> 前进 / 停止 / 后退
```

模拟器会读取这些命令并回传状态；经过脚本内预设的管道问题距离点时，才会产生报警和自动缺陷记录。

模拟器相关位置：

```text
scripts/simulate_robot_sensor.py       模拟机器人传感器上报
frontend/src/App.vue                   SIMULATION-ONLY 模拟模式标识
frontend/src/style.css                 模拟模式和数据入库样式
```

后续接入真实机器人时：

1. 删除或停用 `scripts/simulate_robot_sensor.py`。
2. 将真实嵌入式控制板或算法模块的数据上报到 `POST /api/robot/status`。
3. 将真实巡检数据保存到 `POST /api/inspection/report` 或替换为真实采集流程。
4. 如果不再需要模拟标识，可删除 `App.vue` 中带 `SIMULATION-ONLY` 注释的 `simulationActive` 逻辑。

## 9. 常见问题

### Docker 报 `dockerDesktopLinuxEngine` 找不到

说明 Docker Desktop 没有启动。

处理方式：

1. 打开 Docker Desktop。
2. 等待 Docker Engine Running。
3. 再执行：

```powershell
docker compose up -d postgres
```

### 后端提示数据库表不存在

例如：

```text
relation "pipe_segments" does not exist
```

执行：

```powershell
cd D:\PythonProjects\pipescan\backend
..\.venv\Scripts\python.exe -c "from database import init_db; print(init_db())"
```

然后重启后端。

### 前端打不开或空白

确认你打开的是：

```text
http://localhost:5317
```

不要直接双击：

```text
frontend/index.html
```

Vue/Vite 项目必须通过 Vite 服务访问。

### 后端接口请求失败

先确认后端是否启动：

```text
http://localhost:8000/docs
```

如果打不开，重新启动后端。

### 生成报告失败

按顺序检查：

1. PostgreSQL 容器是否运行：

```powershell
docker compose ps
```

2. 后端数据库连接是否正常：

```text
http://localhost:8000/api/db/health
```

3. 表是否初始化：

```powershell
cd D:\PythonProjects\pipescan\backend
..\.venv\Scripts\python.exe -c "from database import init_db; print(init_db())"
```

## 10. 停止项目

停止前端和后端：

```text
在对应终端按 Ctrl + C
```

停止数据库：

```powershell
cd D:\PythonProjects\pipescan
docker compose stop postgres
```

完全停止并删除容器，但保留数据：

```powershell
docker compose down
```

完全删除数据库数据：

```powershell
docker compose down -v
```

注意：`docker compose down -v` 会删除数据库数据卷，所有已保存的检测数据和报告记录都会丢失。
