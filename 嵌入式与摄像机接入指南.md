# PipeScan 嵌入式控制与摄像机接入指南

本文档说明当前前后端接口如何与真实嵌入式运动控制板、摄像机和检测数据链路结合。当前项目已经预留了软件接口，后续主要工作是把 `backend/main.py` 中的模拟适配器替换为真实硬件通信实现。

## 1. 当前系统数据流

```text
工程师前端 Vue 控制面板
        |
        | HTTP JSON
        v
FastAPI 后端 backend/main.py
        |
        | 运动控制适配器 MotionController
        | 摄像机适配器 CameraAdapter
        | 评估算法 PipeDefectEvaluator
        | 报告生成器 ReportGenerator
        v
嵌入式运动控制板 / 摄像机 / 检测算法 / 报告文件
```

当前前端不直接连接硬件，所有硬件操作都通过后端统一转发。这样做的好处是：

- 前端只关心业务操作，不需要知道串口、CAN、RTSP、SDK 等细节。
- 后端可以做权限校验、日志记录、异常保护和指令限幅。
- 后续更换控制板或摄像机时，前端接口可以保持不变。

## 2. 运动控制接口如何接入嵌入式

### 2.1 当前后端接口

当前运动控制相关接口在 `backend/main.py`：

```text
POST /api/robot/connect
GET  /api/robot/status
POST /api/robot/motion
```

前端点击控制按钮时，会调用：

```http
POST /api/robot/motion
Content-Type: application/json
```

请求体示例：

```json
{
  "action": "forward",
  "speed": 0.5,
  "duration_ms": 300
}
```

字段说明：

| 字段 | 含义 | 示例 |
| --- | --- | --- |
| `action` | 运动动作 | `forward`、`backward`、`left`、`right`、`stop`、`set_speed` |
| `speed` | 速度比例，范围 0 到 1 | `0.5` |
| `duration_ms` | 本次动作持续时间，毫秒 | `300` |

返回体示例：

```json
{
  "connected": true,
  "mode": "manual",
  "speed": 0.5,
  "battery_percent": 86,
  "distance_m": 1.2,
  "last_command": "forward",
  "updated_at": "2026-05-21T14:00:00"
}
```

### 2.2 需要替换的代码位置

当前模拟运动控制在 `backend/main.py`：

```python
class MotionController:
    def connect(self) -> RobotState:
        ...

    def send_command(self, command: MotionCommand) -> RobotState:
        ...
```

后续接入嵌入式时，建议不要把串口/CAN/TCP 代码全部堆进 `main.py`，而是新建硬件模块：

```text
backend/
├─ hardware/
│  ├─ __init__.py
│  ├─ motion_serial.py
│  ├─ motion_can.py
│  └─ motion_tcp.py
```

然后由 `MotionController` 调用这些模块。

### 2.3 串口接入方案

如果控制板通过 USB 转串口通信，建议使用 `pyserial`。

依赖：

```bash
pip install pyserial
```

推荐新增文件：

```text
backend/hardware/motion_serial.py
```

示例结构：

```python
import json
import serial


class SerialMotionClient:
    def __init__(self, port="COM3", baudrate=115200, timeout=0.2):
        self.port = port
        self.baudrate = baudrate
        self.timeout = timeout
        self.serial = None

    def connect(self):
        self.serial = serial.Serial(
            port=self.port,
            baudrate=self.baudrate,
            timeout=self.timeout,
        )

    def send_motion(self, action: str, speed: float, duration_ms: int):
        if self.serial is None or not self.serial.is_open:
            self.connect()

        frame = {
            "type": "motion",
            "action": action,
            "speed": speed,
            "duration_ms": duration_ms,
        }
        payload = json.dumps(frame, ensure_ascii=False) + "\n"
        self.serial.write(payload.encode("utf-8"))

        response = self.serial.readline().decode("utf-8", errors="ignore").strip()
        return json.loads(response) if response else {}
```

嵌入式侧可以接收一行 JSON：

```json
{"type":"motion","action":"forward","speed":0.5,"duration_ms":300}
```

并返回状态：

```json
{"ok":true,"battery_percent":86,"distance_m":1.2,"mode":"manual"}
```

然后修改 `backend/main.py`：

```python
from hardware.motion_serial import SerialMotionClient


class MotionController:
    def __init__(self) -> None:
        self.client = SerialMotionClient(port="COM3", baudrate=115200)
        self.state = RobotState(updated_at=datetime.now().isoformat(timespec="seconds"))

    def connect(self) -> RobotState:
        self.client.connect()
        self.state.connected = True
        self.state.mode = "ready"
        self.state.updated_at = datetime.now().isoformat(timespec="seconds")
        return self.state

    def send_command(self, command: MotionCommand) -> RobotState:
        result = self.client.send_motion(command.action, command.speed, command.duration_ms)
        self.state.connected = result.get("ok", True)
        self.state.last_command = command.action
        self.state.speed = command.speed if command.action != "stop" else 0
        self.state.mode = result.get("mode", "manual")
        self.state.battery_percent = result.get("battery_percent", self.state.battery_percent)
        self.state.distance_m = result.get("distance_m", self.state.distance_m)
        self.state.updated_at = datetime.now().isoformat(timespec="seconds")
        return self.state
```

### 2.4 TCP/以太网接入方案

如果嵌入式控制器提供 TCP 服务，可以用 socket。

推荐协议仍然使用 JSON 行协议：

```text
后端 -> 控制板:
{"type":"motion","action":"forward","speed":0.5,"duration_ms":300}\n

控制板 -> 后端:
{"ok":true,"battery_percent":86,"distance_m":1.2,"mode":"manual"}\n
```

推荐新增：

```text
backend/hardware/motion_tcp.py
```

示例：

```python
import json
import socket


class TcpMotionClient:
    def __init__(self, host="192.168.1.50", port=9000, timeout=0.5):
        self.host = host
        self.port = port
        self.timeout = timeout

    def send_motion(self, action: str, speed: float, duration_ms: int):
        frame = {
            "type": "motion",
            "action": action,
            "speed": speed,
            "duration_ms": duration_ms,
        }
        payload = (json.dumps(frame) + "\n").encode("utf-8")

        with socket.create_connection((self.host, self.port), timeout=self.timeout) as sock:
            sock.sendall(payload)
            data = sock.recv(4096)

        return json.loads(data.decode("utf-8"))
```

### 2.5 CAN 总线接入方案

如果底盘控制使用 CAN，总体建议是：

- 后端 HTTP 指令转为标准 CAN 帧。
- 控制板周期性上报电量、速度、里程、姿态、故障码。
- 后端维护最新机器人状态，前端定时读取 `/api/robot/status`。

推荐 CAN 帧规划：

| CAN ID | 方向 | 含义 |
| --- | --- | --- |
| `0x101` | 后端到控制板 | 运动控制 |
| `0x102` | 后端到控制板 | 停止/急停 |
| `0x201` | 控制板到后端 | 机器人状态 |
| `0x202` | 控制板到后端 | 故障码 |
| `0x203` | 控制板到后端 | 里程/姿态 |

运动控制帧示例：

| Byte | 含义 |
| --- | --- |
| 0 | 动作编号，1 前进，2 后退，3 左转，4 右转，5 停止 |
| 1 | 速度，0 到 100 |
| 2-3 | 持续时间 ms，高低字节 |
| 4-7 | 保留 |

## 3. 建议的嵌入式通信协议

为了早期调试方便，建议第一版使用 JSON 行协议。等功能稳定后，如果有实时性或带宽要求，再改为二进制协议。

### 3.1 指令格式

```json
{
  "type": "motion",
  "seq": 1001,
  "action": "forward",
  "speed": 0.5,
  "duration_ms": 300,
  "timestamp": "2026-05-21T14:00:00"
}
```

### 3.2 状态返回格式

```json
{
  "type": "robot_status",
  "seq": 1001,
  "ok": true,
  "mode": "manual",
  "battery_percent": 86,
  "speed": 0.5,
  "distance_m": 1.2,
  "temperature_c": 42.5,
  "error_code": 0
}
```

### 3.3 故障返回格式

```json
{
  "type": "error",
  "seq": 1001,
  "ok": false,
  "error_code": 3002,
  "message": "motor driver overcurrent"
}
```

### 3.4 必须支持的安全指令

嵌入式侧必须优先支持：

```json
{"type":"motion","action":"stop","speed":0,"duration_ms":0}
```

建议后续新增单独接口：

```text
POST /api/robot/emergency-stop
```

急停接口不应排队，后端收到后应立即发送到底层控制板。

## 4. 摄像机如何接入

### 4.1 当前后端接口

摄像机相关接口在 `backend/main.py`：

```text
GET  /api/camera
POST /api/camera
GET  /api/camera/snapshot
```

当前 `CameraAdapter` 只是模拟实现：

```python
class CameraAdapter:
    def update(self, config: CameraConfig) -> CameraConfig:
        ...

    def snapshot(self) -> dict[str, str | bool]:
        ...
```

后续真实接入时，应替换 `CameraAdapter` 内部逻辑。

### 4.2 USB 摄像头接入

如果摄像头直接接在运行后端的电脑或工控机上，可以使用 OpenCV。

依赖：

```bash
pip install opencv-python
```

推荐新增：

```text
backend/hardware/camera_usb.py
```

示例：

```python
import cv2
from pathlib import Path
from datetime import datetime


class UsbCameraClient:
    def __init__(self, device_index=0):
        self.device_index = device_index
        self.capture = cv2.VideoCapture(device_index)

    def snapshot(self, output_dir: Path):
        output_dir.mkdir(parents=True, exist_ok=True)
        ok, frame = self.capture.read()
        if not ok:
            raise RuntimeError("读取 USB 摄像头失败")

        filename = f"frame_{datetime.now().strftime('%Y%m%d_%H%M%S')}.jpg"
        path = output_dir / filename
        cv2.imwrite(str(path), frame)
        return path
```

然后在 `CameraAdapter.snapshot` 里调用：

```python
path = self.client.snapshot(Path(__file__).parent / "frames")
return {
    "enabled": True,
    "source": "usb://0",
    "resolution": self.config.resolution,
    "frame_url": f"/frames/{path.name}",
    "captured_at": datetime.now().isoformat(timespec="seconds"),
}
```

注意：如果要让前端直接显示截图，还需要在 FastAPI 中挂载静态目录。

```python
from fastapi.staticfiles import StaticFiles

app.mount("/frames", StaticFiles(directory=Path(__file__).parent / "frames"), name="frames")
```

### 4.3 RTSP 网络摄像头接入

如果机器人摄像头通过网络推流，常见地址如下：

```text
rtsp://用户名:密码@摄像头IP:554/stream1
```

OpenCV 读取示例：

```python
import cv2

capture = cv2.VideoCapture("rtsp://admin:123456@192.168.1.60:554/stream1")
ok, frame = capture.read()
```

推荐配置：

```json
{
  "source": "rtsp://admin:123456@192.168.1.60:554/stream1",
  "enabled": true,
  "resolution": "1280x720"
}
```

前端已经有摄像头配置区域，会通过：

```text
POST /api/camera
```

把 `source` 传给后端。

### 4.4 视频实时预览方案

当前前端摄像头区域只是占位展示。后续可以选择三种方案。

#### 方案 A：MJPEG，最简单

后端不断读取摄像头帧，并通过 HTTP 流输出：

```text
GET /api/camera/stream
```

前端使用：

```html
<img src="http://localhost:8000/api/camera/stream" />
```

优点：

- 实现简单。
- 浏览器兼容好。
- 适合调试和低帧率预览。

缺点：

- 延迟和带宽表现一般。
- 不适合高清高帧率。

#### 方案 B：WebRTC，实时性最好

适合低延迟远程驾驶。

优点：

- 延迟低。
- 浏览器原生支持。

缺点：

- 后端实现复杂。
- 需要信令服务和媒体管道。

#### 方案 C：RTSP 转 HLS/FLV

适合工程展示和网页观看。

优点：

- 比 MJPEG 更适合长时间视频。

缺点：

- HLS 延迟通常较高。
- FLV 需要前端播放器库。

项目早期建议先使用 MJPEG，稳定后再升级 WebRTC。

## 5. 检测数据如何进入报告算法

当前报告算法需要的缺陷列表格式：

```json
{
  "pipe_id": "P-001",
  "pipe_length": 30,
  "diameter_mm": 800,
  "region_type": "traffic",
  "soil_type": "medium",
  "defects": [
    {
      "category": "structural",
      "code": "PL",
      "score": 5,
      "length": 0.8,
      "distance_m": 6.4,
      "description": "管壁破裂"
    }
  ]
}
```

这里的缺陷数据可以来自三种方式：

### 5.1 工程师人工录入

当前前端已经支持这种方式。工程师看到视频后，手动填写：

- 缺陷距离
- 缺陷类型
- 缺陷代码
- 分值
- 长度
- 描述

然后点击“生成报告”。

### 5.2 嵌入式或机器人主动上报

可以新增接口：

```text
POST /api/inspection/defects
```

机器人每发现一个缺陷，就上报：

```json
{
  "pipe_id": "P-001",
  "distance_m": 6.4,
  "category": "structural",
  "code": "PL",
  "score": 5,
  "length": 0.8,
  "description": "AI识别疑似管壁破裂",
  "frame_url": "/frames/frame_20260521_140000.jpg",
  "confidence": 0.91
}
```

后端保存到内存或数据库，前端定时刷新。

### 5.3 AI 视觉算法自动识别

如果后续加入图像识别模型，建议流程如下：

```text
摄像头帧
  -> 图像识别模型
  -> 缺陷候选结果
  -> 结合机器人里程 distance_m
  -> 缺陷合并与去重
  -> 写入 defects
  -> evaluator.py 计算风险
  -> reportgenerator.py 生成报告
```

AI 输出建议包含：

| 字段 | 含义 |
| --- | --- |
| `category` | 结构性或功能性 |
| `code` | 缺陷代码 |
| `score` | 按规范映射后的分值 |
| `confidence` | 模型置信度 |
| `distance_m` | 当前机器人里程 |
| `frame_url` | 缺陷截图路径 |
| `bbox` | 图像中的缺陷位置 |

## 6. 建议新增的后端文件结构

后续建议逐步整理为：

```text
backend/
├─ main.py
├─ evaluator.py
├─ reportgenerator.py
├─ hardware/
│  ├─ __init__.py
│  ├─ motion_serial.py
│  ├─ motion_tcp.py
│  ├─ motion_can.py
│  ├─ camera_usb.py
│  └─ camera_rtsp.py
├─ services/
│  ├─ robot_service.py
│  ├─ camera_service.py
│  └─ inspection_service.py
├─ storage/
│  ├─ frames/
│  └─ reports/
└─ models/
   └─ schemas.py
```

目前项目较小，所有 API 暂时放在 `main.py` 是可以的。等硬件接入后，建议逐步拆分。

## 7. 联调步骤建议

### 第一步：确认后端和前端能运行

后端：

```bash
cd backend
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

前端：

```bash
cd frontend
npm run dev
```

访问：

```text
http://localhost:5317
```

### 第二步：用模拟接口确认流程

在前端点击：

1. 连接机器人
2. 前进、后退、停止
3. 填写缺陷
4. 计算风险
5. 生成报告

确认软件流程正常后再接硬件。

### 第三步：接入运动控制板

优先建议串口接入，原因是调试简单。

需要确认：

- 串口号，例如 `COM3`
- 波特率，例如 `115200`
- 指令协议
- 状态返回格式
- 急停指令优先级

### 第四步：接入摄像头

先确认摄像头类型：

- USB 摄像头：使用 OpenCV 读取设备编号。
- RTSP 摄像头：确认 RTSP 地址、用户名、密码。
- 厂商 SDK：确认 Python 是否可调用 SDK。

### 第五步：打通里程与视频帧

报告中最重要的是缺陷位置。建议后端维护：

```text
当前机器人里程 distance_m
当前视频帧 frame_url
当前时间戳 timestamp
```

发现缺陷时，把三者一起保存：

```json
{
  "distance_m": 6.4,
  "frame_url": "/frames/frame_20260521_140000.jpg",
  "captured_at": "2026-05-21T14:00:00"
}
```

## 8. 安全与工程注意事项

### 8.1 后端必须做限幅

即使前端限制速度范围，后端仍然要限制：

```text
0 <= speed <= 1
0 <= duration_ms <= 60000
```

当前 `MotionCommand` 已经使用 Pydantic 做了基础限制。

### 8.2 必须支持停止和急停

停止指令：

```json
{"action":"stop","speed":0,"duration_ms":0}
```

建议嵌入式固件中将停止指令设为最高优先级。

### 8.3 通信异常时自动停车

建议控制板实现心跳超时机制：

```text
如果 500ms 或 1000ms 内没有收到后端心跳或运动指令，自动停车。
```

### 8.4 摄像头断流要可恢复

摄像头读取失败时，后端不要直接崩溃，应返回：

```json
{
  "enabled": false,
  "message": "camera disconnected"
}
```

前端显示错误状态，工程师可以重新配置视频源。

## 9. 推荐开发顺序

1. 固定运动控制协议。
2. 用串口助手或 TCP 调试工具验证控制板指令。
3. 在后端新增 `hardware/motion_serial.py` 或 `hardware/motion_tcp.py`。
4. 替换 `MotionController.send_command`。
5. 接入摄像头截图接口。
6. 增加视频流接口。
7. 把缺陷截图和里程写入报告。
8. 引入数据库保存历史检测任务。
9. 增加用户权限、日志和急停接口。

## 10. 当前项目中最先要改的地方

运动控制：

```text
backend/main.py
MotionController.connect
MotionController.send_command
```

摄像头：

```text
backend/main.py
CameraAdapter.update
CameraAdapter.snapshot
```

报告算法：

```text
backend/evaluator.py
PipeDefectEvaluator.evaluate
```

报告格式：

```text
backend/reportgenerator.py
ReportGenerator.generate_markdown
```

前端展示：

```text
frontend/src/App.vue
```

