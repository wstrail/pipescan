# PipeScan 嵌入式接口说明

本文档面向嵌入式控制板、摄像机、算法盒子或机器人底盘同学，说明为了让手机软件能够操作机器人，嵌入式侧需要提供哪些通信信息、控制协议、状态数据和安全能力。

当前项目的硬件接口仍是模拟实现：前端通过 HTTP 调后端，后端在 `backend/main.py` 中用 `MotionController` 和 `CameraAdapter` 保存模拟状态。后续接真实硬件时，主要替换这两个适配器内部逻辑即可，前端接口可以保持不变。

## 1. 对接目标

手机软件要能控制机器人，必须先建立这条链路：

```text
手机软件
    |
    | Wi-Fi / 局域网 / 4G / 5G
    v
后端服务
    |
    | 串口 / TCP / UDP / CAN / SDK / MQTT
    v
嵌入式控制板
    |
    v
电机、灯光、摄像头、传感器
```

嵌入式同学需要提供的不是前端页面，而是机器人底层如何通信、如何执行命令、如何返回状态。软件侧拿到这些内容后，才能把手机按钮上的“前进、后退、停止”等操作转换成机器人能识别的指令。

## 2. 嵌入式同学需要提供的内容清单

### 2.1 通信方式

请明确机器人控制板支持哪种通信方式。

| 内容 | 示例 | 是否必须 |
| --- | --- | --- |
| 通信方式 | 串口、TCP、UDP、CAN、BLE、MQTT、厂商 SDK | 必须 |
| 连接位置 | 控制板直连后端电脑、机器人自带工控机、手机直连机器人热点 | 必须 |
| 物理接口 | USB 转串口、网口、Wi-Fi、CAN 盒 | 必须 |
| 是否需要登录或鉴权 | token、账号密码、无鉴权 | 建议提供 |

如果使用串口，请提供：

| 内容 | 示例 |
| --- | --- |
| 串口号 | Windows: `COM3`，Linux: `/dev/ttyUSB0` |
| 波特率 | `115200` |
| 数据位 | `8` |
| 停止位 | `1` |
| 校验位 | `None` |
| 编码 | UTF-8 或二进制 |
| 分包方式 | 换行符 `\n`、固定长度、帧头帧尾 |

如果使用 TCP/UDP，请提供：

| 内容 | 示例 |
| --- | --- |
| 控制板 IP | `192.168.1.50` |
| 端口 | `9000` |
| 机器人是否作为 Server | 是，软件主动连接机器人 |
| 是否长连接 | 长连接或每条命令短连接 |
| 超时时间 | `500ms`、`1000ms` |
| 断线后是否自动重连 | 是或否 |

如果使用 CAN，请提供：

| 内容 | 示例 |
| --- | --- |
| CAN 波特率 | `500kbps` |
| 发送 CAN ID | `0x101` |
| 状态 CAN ID | `0x201` |
| 急停 CAN ID | `0x102` |
| 每个字节含义 | Byte0 动作，Byte1 速度 |

### 2.2 控制命令协议

请提供机器人能识别的命令格式，至少包括：

| 手机操作 | 嵌入式动作 | 必须支持 |
| --- | --- | --- |
| 前进 | `forward` | 是 |
| 后退 | `backward` | 是 |
| 左转 | `left` | 是 |
| 右转 | `right` | 是 |
| 停止 | `stop` | 是 |
| 设置速度 | `set_speed` | 建议 |
| 急停 | `emergency_stop` | 强烈建议 |

如果嵌入式侧支持 JSON，推荐使用：

```json
{
  "type": "motion",
  "seq": 1001,
  "action": "forward",
  "speed": 0.5,
  "duration_ms": 300
}
```

如果嵌入式侧使用二进制协议，请提供完整协议表，例如：

| 字节 | 含义 | 示例 |
| --- | --- | --- |
| Byte0 | 帧头 | `0xAA` |
| Byte1 | 动作编号 | `0x01` 表示前进 |
| Byte2 | 速度 | `0` 到 `100` |
| Byte3-4 | 持续时间 ms | 高字节在前 |
| Byte5 | 校验和 | 前面字节求和 |
| Byte6 | 帧尾 | `0x55` |

### 2.3 动作和速度映射

软件侧只知道 `speed` 是 `0` 到 `1` 的比例，嵌入式侧需要说明如何映射到实际电机控制。

请提供：

| 内容 | 示例 |
| --- | --- |
| `speed=0` 的含义 | 停止 |
| `speed=1` 的含义 | 最大安全速度 |
| 最小可动作速度 | 例如 `0.15` 以下电机不动 |
| 最大允许速度 | 例如限制为 `0.8` |
| 速度单位 | PWM、rpm、m/s 或百分比 |
| 左右轮速度关系 | 差速控制表 |
| 转向方式 | 原地差速转向、舵机转向、履带转向 |

示例：

| 软件动作 | 左轮 | 右轮 |
| --- | --- | --- |
| `forward` | `+speed` | `+speed` |
| `backward` | `-speed` | `-speed` |
| `left` | `-speed` | `+speed` |
| `right` | `+speed` | `-speed` |
| `stop` | `0` | `0` |

### 2.4 状态上报协议

手机软件需要看到机器人是否在线、电量、速度、里程、故障等信息。嵌入式侧需要提供状态返回格式。

最少建议提供：

| 字段 | 含义 | 是否必须 |
| --- | --- | --- |
| `connected` | 是否在线 | 必须 |
| `mode` | 当前模式 | 必须 |
| `battery_percent` | 电量百分比 | 必须 |
| `speed` | 当前速度 | 必须 |
| `distance_m` | 当前里程 | 强烈建议 |
| `last_command` | 最近执行命令 | 建议 |
| `error_code` | 故障码 | 强烈建议 |
| `message` | 故障描述 | 建议 |
| `temperature_c` | 控制板或电机温度 | 建议 |
| `pitch_deg` / `roll_deg` | 姿态角 | 可选 |
| `obstacle_detected` | 前方是否检测到障碍或严重破损阻挡 | 强烈建议 |
| `obstacle_distance_m` | 障碍距离，单位 m | 建议 |
| `obstacle_severity` | 障碍等级：`none`、`warning`、`critical` | 建议 |
| `obstacle_message` | 给控制员看的障碍说明 | 建议 |

推荐返回：

```json
{
  "type": "robot_status",
  "seq": 1001,
  "ok": true,
  "mode": "manual",
  "battery_percent": 86,
  "speed": 0.5,
  "distance_m": 1.2,
  "error_code": 0,
  "message": "",
  "obstacle_detected": false,
  "obstacle_distance_m": null,
  "obstacle_severity": "none",
  "obstacle_message": ""
}
```

当前软件已经支持自动读入机器人状态：

- 嵌入式或硬件适配层可以调用 `POST /api/robot/status` 主动写入最新状态。
- 手机/前端控制台会每 1 秒读取一次 `GET /api/robot/status`。
- 电量 `battery_percent`、里程 `distance_m`、速度 `speed`、模式 `mode` 等字段变化后，页面会自动跟随变化。
- 当前方有障碍或管道破损严重影响通行时，嵌入式侧应上报 `obstacle_detected: true`；控制面板会在视频区域和运动控制面板直接显示告警，提醒控制员减速、观察或停止。

嵌入式主动上报示例：

```http
POST /api/robot/status
Content-Type: application/json
```

```json
{
  "connected": true,
  "mode": "manual",
  "battery_percent": 73,
  "speed": 0.35,
  "distance_m": 12.8,
  "last_command": "forward",
  "error_code": 0,
  "message": "",
  "temperature_c": 42.5,
  "pitch_deg": 1.2,
  "roll_deg": -0.4,
  "obstacle_detected": true,
  "obstacle_distance_m": 0.65,
  "obstacle_severity": "critical",
  "obstacle_message": "前方管道破损严重，疑似塌陷阻挡，请立即停止并复核。"
}
```

软件返回并保存为当前状态：

```json
{
  "connected": true,
  "mode": "manual",
  "speed": 0.35,
  "battery_percent": 73,
  "distance_m": 12.8,
  "last_command": "forward",
  "error_code": 0,
  "message": "",
  "temperature_c": 42.5,
  "pitch_deg": 1.2,
  "roll_deg": -0.4,
  "obstacle_detected": true,
  "obstacle_distance_m": 0.65,
  "obstacle_severity": "critical",
  "obstacle_message": "前方管道破损严重，疑似塌陷阻挡，请立即停止并复核。",
  "updated_at": "2026-05-26T21:40:00"
}
```

### 2.5 安全机制

这是手机遥控机器人必须确认的内容。

嵌入式侧需要说明并实现：

| 安全项 | 要求 |
| --- | --- |
| 停止命令 | 收到 `stop` 后立即停止 |
| 急停命令 | 优先级最高，不排队 |
| 通信超时停车 | 例如 `500ms` 到 `1000ms` 未收到命令自动停车 |
| 速度限幅 | 控制板二次限制最大速度 |
| 故障停车 | 过流、欠压、堵转、姿态异常时自动停车 |
| 上电默认状态 | 上电后默认不运动 |
| 丢包处理 | 命令丢失或校验失败不执行 |

### 2.6 摄像头和视频流

如果手机软件需要看机器人画面，嵌入式或机器人端还需要提供摄像头信息。

请提供：

| 内容 | 示例 |
| --- | --- |
| 摄像头类型 | USB、RTSP 网络摄像头、MIPI、厂商 SDK |
| 视频地址 | `rtsp://admin:123456@192.168.1.60:554/stream1` |
| 分辨率 | `1280x720` |
| 帧率 | `25fps` |
| 编码格式 | H.264、MJPEG |
| 是否需要账号密码 | 是或否 |
| 是否支持截图 | 是或否 |

### 2.7 网络连接方式

为了让手机软件连上机器人，需要确认实际网络拓扑。

常见方案：

| 方案 | 说明 | 需要嵌入式提供 |
| --- | --- | --- |
| 手机和机器人在同一 Wi-Fi | 手机访问后端 IP | 机器人或工控机 IP |
| 机器人开热点 | 手机连接机器人热点 | 热点名称、密码、后端 IP |
| 后端部署在云服务器 | 手机和机器人都连云端 | 机器人主动连接云端协议 |
| 手机蓝牙直连机器人 | 手机直接通过 BLE 控制 | BLE 服务 UUID、特征值 UUID、数据格式 |

当前项目更适合第一种或第二种：手机访问后端，后端再控制机器人。

### 2.8 调试资料

嵌入式同学最好同时提供：

- 一份通信协议文档。
- 一份动作编号表。
- 一份状态字段表。
- 一份错误码表。
- 串口或 TCP 调试示例。
- 控制板模拟器或测试固件。
- 正常前进、停止、断线、故障场景的返回样例。
- 控制板固件版本号和更新方式。

## 3. 建议交付格式

嵌入式同学可以按下面模板提供。

```text
1. 通信方式
   - 类型：TCP
   - 控制板 IP：192.168.1.50
   - 端口：9000
   - 协议：JSON Lines
   - 超时：1000ms

2. 控制命令
   - forward：前进
   - backward：后退
   - left：左转
   - right：右转
   - stop：停止
   - emergency_stop：急停

3. 命令示例
   {"type":"motion","seq":1,"action":"forward","speed":0.5,"duration_ms":300}

4. 返回示例
   {"type":"robot_status","seq":1,"ok":true,"battery_percent":86,"distance_m":1.2}

5. 安全策略
   - 1000ms 未收到命令自动停车
   - stop 和 emergency_stop 最高优先级
   - 最大速度限制为 0.8

6. 摄像头
   - 类型：RTSP
   - 地址：rtsp://admin:123456@192.168.1.60:554/stream1
   - 分辨率：1280x720
```

## 4. 系统对接边界

```text
前端控制台(Vue)
    |
    | HTTP JSON
    v
后端服务(FastAPI, backend/main.py)
    |
    | 待接入：串口 / TCP / CAN / 厂商 SDK
    v
嵌入式运动控制板 / 摄像机 / 算法模块
```

嵌入式侧不需要直接对接前端页面。推荐由后端统一转发命令、保存状态、生成报告。

## 5. 后端服务地址

本地开发默认地址：

```text
http://localhost:8000
```

如果部署到工控机或机器人主机上，请把 `localhost` 替换为后端所在机器 IP，例如：

```text
http://192.168.1.10:8000
```

健康检查：

```http
GET /api/health
```

返回示例：

```json
{
  "status": "ok",
  "time": "2026-05-26T14:30:00"
}
```

## 6. 运动控制接口

### 3.1 连接机器人

```http
POST /api/robot/connect
```

当前作用：把后端内存中的机器人状态设置为已连接。

返回示例：

```json
{
  "connected": true,
  "mode": "ready",
  "speed": 0.0,
  "battery_percent": 86,
  "distance_m": 0.0,
  "last_command": "none",
  "updated_at": "2026-05-26T14:30:00"
}
```

接入真实硬件后，建议在该接口里执行：

- 打开串口、TCP 连接或 CAN 设备。
- 读取一次控制板状态。
- 如果控制板在线，返回 `connected: true`。
- 如果连接失败，后端应返回明确错误信息。

### 3.2 查询机器人状态

```http
GET /api/robot/status
```

返回字段：

| 字段 | 类型 | 含义 |
| --- | --- | --- |
| `connected` | boolean | 后端是否认为机器人已连接 |
| `mode` | string | 当前模式，例如 `standby`、`ready`、`manual`、`stopped` |
| `speed` | number | 当前速度比例，范围 `0` 到 `1` |
| `battery_percent` | integer | 电量百分比 |
| `distance_m` | number | 当前累计里程，单位 m |
| `last_command` | string | 最近一次运动命令 |
| `updated_at` | string | 状态更新时间 |

返回示例：

```json
{
  "connected": true,
  "mode": "manual",
  "speed": 0.5,
  "battery_percent": 86,
  "distance_m": 1.2,
  "last_command": "forward",
  "updated_at": "2026-05-26T14:31:00"
}
```

### 3.3 发送运动命令

```http
POST /api/robot/motion
Content-Type: application/json
```

请求体：

```json
{
  "action": "forward",
  "speed": 0.5,
  "duration_ms": 300
}
```

请求字段：

| 字段 | 类型 | 必填 | 限制 | 含义 |
| --- | --- | --- | --- | --- |
| `action` | string | 是 | 见动作枚举 | 运动动作 |
| `speed` | number | 否 | `0 <= speed <= 1` | 速度比例 |
| `duration_ms` | integer | 否 | `0 <= duration_ms <= 60000` | 动作持续时间，单位 ms |

动作枚举：

| `action` | 中文含义 | 建议嵌入式动作 |
| --- | --- | --- |
| `forward` | 前进 | 左右轮同向前进 |
| `backward` | 后退 | 左右轮同向后退 |
| `left` | 左转 | 差速左转或舵机左转 |
| `right` | 右转 | 差速右转或舵机右转 |
| `stop` | 停止 | 立即停止电机输出 |
| `set_speed` | 设置速度 | 只更新速度参数，不改变方向 |

返回体同 `/api/robot/status`。

前端当前点击方向键时，会发送类似请求：

```json
{
  "action": "forward",
  "speed": 0.35,
  "duration_ms": 300
}
```

注意事项：

- `stop` 必须作为最高优先级命令处理。
- `speed` 是归一化速度，不是实际 PWM 或 rpm。嵌入式侧需要自行映射。
- `duration_ms` 当前前端默认为 `0`，表示持续执行当前动作，直到收到下一条动作指令或 `stop`。
- 如果真实控制板需要看门狗保护，建议由嵌入式侧设置心跳超时自动停止，同时软件侧保持周期性状态读取。
- 如果后续需要连续运动，可以让前端周期性发送运动命令，也可以由后端新增心跳机制。

## 7. 摄像机接口

### 4.1 查询摄像机配置

```http
GET /api/camera
```

返回示例：

```json
{
  "source": "mock://camera-0",
  "enabled": true,
  "resolution": "1280x720"
}
```

### 4.2 设置摄像机配置

```http
POST /api/camera
Content-Type: application/json
```

请求体：

```json
{
  "source": "rtsp://admin:123456@192.168.1.60:554/stream1",
  "enabled": true,
  "resolution": "1280x720"
}
```

字段说明：

| 字段 | 类型 | 含义 |
| --- | --- | --- |
| `source` | string | 视频源，可为 `mock://camera-0`、`usb://0`、RTSP 地址或厂商 SDK 标识 |
| `enabled` | boolean | 是否启用摄像机 |
| `resolution` | string | 分辨率字符串，例如 `1280x720` |

### 4.3 获取当前帧信息

```http
GET /api/camera/snapshot
```

当前返回的是占位信息：

```json
{
  "enabled": true,
  "source": "mock://camera-0",
  "resolution": "1280x720",
  "frame_url": "/api/camera/mock-frame",
  "captured_at": "2026-05-26T14:32:00"
}
```

接真实摄像机后，建议后端返回真实截图地址：

```json
{
  "enabled": true,
  "source": "rtsp://admin:123456@192.168.1.60:554/stream1",
  "resolution": "1280x720",
  "frame_url": "/frames/frame_20260526_143200.jpg",
  "captured_at": "2026-05-26T14:32:00"
}
```

## 8. 检测与报告接口

嵌入式或算法模块如果要把缺陷结果交给系统生成报告，当前可以直接调用整单评估接口。

### 5.1 只计算风险等级

```http
POST /api/inspection/evaluate
Content-Type: application/json
```

请求示例：

```json
{
  "pipe_id": "P-001",
  "pipe_length": 30,
  "diameter_mm": 800,
  "region_type": "traffic",
  "soil_type": "medium",
  "location": "A路-B路",
  "remark": "机器人巡检",
  "defects": [
    {
      "category": "structural",
      "code": "PL",
      "score": 5,
      "length": 0.8,
      "distance_m": 6.4,
      "description": "管壁破裂"
    }
  ]
}
```

### 5.2 计算并生成报告

```http
POST /api/inspection/report
Content-Type: application/json
```

请求体同 `/api/inspection/evaluate`。返回结果包含评估结果、报告 Markdown、报告路径和数据库记录 ID。

缺陷字段说明：

| 字段 | 类型 | 限制 | 含义 |
| --- | --- | --- | --- |
| `category` | string | `structural` 或 `functional` | 结构性缺陷或功能性缺陷 |
| `code` | string | 非空建议 | 缺陷代码 |
| `score` | number | `0 <= score <= 10` | 缺陷分值 |
| `length` | number | `>= 0` | 缺陷长度 |
| `distance_m` | number | `>= 0` | 缺陷所在里程 |
| `description` | string | 可空 | 文字描述 |

当前项目尚未单独实现“嵌入式逐条上报缺陷”的接口。如果算法盒子要实时上报，建议后续新增：

```http
POST /api/inspection/defects
```

建议请求：

```json
{
  "pipe_id": "P-001",
  "distance_m": 6.4,
  "category": "structural",
  "code": "PL",
  "score": 5,
  "length": 0.8,
  "description": "AI识别疑似管壁破裂",
  "frame_url": "/frames/frame_20260526_143200.jpg",
  "confidence": 0.91
}
```

## 9. 后端代码预留位置

当前需要替换的核心位置：

```text
backend/main.py
```

运动控制：

```python
class MotionController:
    def connect(self) -> RobotState:
        ...

    def send_command(self, command: MotionCommand) -> RobotState:
        ...
```

摄像机：

```python
class CameraAdapter:
    def update(self, config: CameraConfig) -> CameraConfig:
        ...

    def snapshot(self) -> dict[str, str | bool]:
        ...
```

推荐新增硬件适配层：

```text
backend/
  hardware/
    __init__.py
    motion_serial.py
    motion_tcp.py
    motion_can.py
    camera_usb.py
    camera_rtsp.py
```

这样 `main.py` 只负责 HTTP API，具体串口、TCP、CAN、SDK 逻辑放在 `hardware` 目录里。

## 10. 推荐嵌入式通信协议

第一版建议使用 JSON Lines 协议，方便串口助手、TCP 调试工具直接验证。

### 7.1 后端发给控制板

每条消息一行，以 `\n` 结束：

```json
{"type":"motion","seq":1001,"action":"forward","speed":0.5,"duration_ms":300,"timestamp":"2026-05-26T14:33:00"}
```

字段说明：

| 字段 | 含义 |
| --- | --- |
| `type` | 消息类型，运动命令固定为 `motion` |
| `seq` | 序号，用于匹配应答 |
| `action` | 动作，使用后端动作枚举 |
| `speed` | 速度比例，范围 `0` 到 `1` |
| `duration_ms` | 持续时间，单位 ms |
| `timestamp` | 后端发送时间 |

### 7.2 控制板返回后端

```json
{"type":"robot_status","seq":1001,"ok":true,"mode":"manual","battery_percent":86,"speed":0.5,"distance_m":1.2,"error_code":0}
```

建议返回字段：

| 字段 | 含义 |
| --- | --- |
| `type` | `robot_status` 或 `error` |
| `seq` | 对应请求序号 |
| `ok` | 命令是否执行成功 |
| `mode` | 当前模式 |
| `battery_percent` | 电量百分比 |
| `speed` | 当前速度比例 |
| `distance_m` | 当前累计里程 |
| `error_code` | 错误码，正常为 `0` |
| `message` | 错误描述，可选 |

错误返回示例：

```json
{"type":"error","seq":1001,"ok":false,"error_code":3002,"message":"motor driver overcurrent"}
```

## 11. 串口接入建议

推荐参数：

| 参数 | 建议值 |
| --- | --- |
| 波特率 | `115200` |
| 数据位 | `8` |
| 停止位 | `1` |
| 校验 | `None` |
| 编码 | UTF-8 |
| 分包 | 一行一个 JSON，以 `\n` 结束 |

后端到控制板：

```text
{"type":"motion","seq":1,"action":"forward","speed":0.5,"duration_ms":300}\n
```

控制板到后端：

```text
{"type":"robot_status","seq":1,"ok":true,"mode":"manual","battery_percent":86,"distance_m":1.2}\n
```

## 12. TCP 接入建议

如果控制板提供 TCP Server，建议：

| 项 | 建议 |
| --- | --- |
| 控制板 IP | 固定 IP，例如 `192.168.1.50` |
| 端口 | 例如 `9000` |
| 协议 | JSON Lines |
| 超时 | 连接超时 `0.5s` 到 `1s` |
| 重连 | 后端失败后可自动重连 |

交互格式与串口一致。

## 13. CAN 接入建议

如果底盘使用 CAN，后端需要将 HTTP 指令转换为 CAN 帧。

建议 CAN ID：

| CAN ID | 方向 | 含义 |
| --- | --- | --- |
| `0x101` | 后端到控制板 | 运动控制 |
| `0x102` | 后端到控制板 | 停止或急停 |
| `0x201` | 控制板到后端 | 机器人状态 |
| `0x202` | 控制板到后端 | 故障码 |
| `0x203` | 控制板到后端 | 里程和姿态 |

运动控制帧建议：

| Byte | 含义 |
| --- | --- |
| 0 | 动作编号：`1` 前进，`2` 后退，`3` 左转，`4` 右转，`5` 停止，`6` 设置速度 |
| 1 | 速度，`0` 到 `100` |
| 2-3 | 持续时间 ms，高字节在前 |
| 4-7 | 保留 |

## 14. 安全要求

嵌入式侧必须优先支持停止命令：

```json
{"type":"motion","action":"stop","speed":0,"duration_ms":0}
```

建议固件实现：

- 停止命令最高优先级，不排队。
- 超过 `500ms` 到 `1000ms` 没收到心跳或运动命令，自动停车。
- 电机过流、欠压、通信异常、姿态异常时主动停车并返回错误。
- 对速度、转向、持续时间做二次限幅，不完全信任上位机输入。
- 控制板上报的 `distance_m` 应作为报告缺陷定位的主要来源。

建议后续新增独立急停接口：

```http
POST /api/robot/emergency-stop
```

当前代码尚未实现该接口，现阶段请使用：

```http
POST /api/robot/motion
Content-Type: application/json

{"action":"stop","speed":0,"duration_ms":0}
```

## 15. 联调步骤

1. 启动后端服务。

```bash
cd backend
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

2. 验证后端在线。

```bash
curl http://localhost:8000/api/health
```

3. 连接机器人。

```bash
curl -X POST http://localhost:8000/api/robot/connect
```

4. 发送前进命令。

```bash
curl -X POST http://localhost:8000/api/robot/motion ^
  -H "Content-Type: application/json" ^
  -d "{\"action\":\"forward\",\"speed\":0.5,\"duration_ms\":300}"
```

5. 发送停止命令。

```bash
curl -X POST http://localhost:8000/api/robot/motion ^
  -H "Content-Type: application/json" ^
  -d "{\"action\":\"stop\",\"speed\":0,\"duration_ms\":0}"
```

6. 查询状态。

```bash
curl http://localhost:8000/api/robot/status
```

7. 接入真实硬件后，观察控制板是否收到同样的动作、速度和持续时间。

## 16. 当前已实现与未实现清单

已实现：

| 能力 | 接口 | 状态 |
| --- | --- | --- |
| 后端健康检查 | `GET /api/health` | 已实现 |
| 连接机器人 | `POST /api/robot/connect` | 已实现，模拟 |
| 查询机器人状态 | `GET /api/robot/status` | 已实现，模拟 |
| 前进/后退/左转/右转/停止/调速 | `POST /api/robot/motion` | 已实现，模拟 |
| 摄像机配置 | `GET /api/camera`、`POST /api/camera` | 已实现，模拟 |
| 摄像机截图信息 | `GET /api/camera/snapshot` | 已实现，占位 |
| 缺陷评估 | `POST /api/inspection/evaluate` | 已实现 |
| 生成报告 | `POST /api/inspection/report` | 已实现 |

建议后续实现：

| 能力 | 建议接口或模块 |
| --- | --- |
| 串口运动控制 | `backend/hardware/motion_serial.py` |
| TCP 运动控制 | `backend/hardware/motion_tcp.py` |
| CAN 运动控制 | `backend/hardware/motion_can.py` |
| USB 摄像机 | `backend/hardware/camera_usb.py` |
| RTSP 摄像机 | `backend/hardware/camera_rtsp.py` |
| 实时视频流 | `GET /api/camera/stream` |
| 独立急停 | `POST /api/robot/emergency-stop` |
| 缺陷逐条上报 | `POST /api/inspection/defects` |

## 17. 嵌入式同学需要确认的信息

对接前请确认：

- 控制板通信方式：串口、TCP、CAN 还是 SDK。
- 串口号、波特率、校验位，或 TCP IP/端口。
- `speed` 到 PWM、rpm 或电机目标速度的映射关系。
- 左转、右转是原地转向、差速转向还是舵机转向。
- 是否由控制板负责 `duration_ms` 到时停止。
- 里程 `distance_m` 由谁计算，精度是多少。
- 电量、故障码、温度、电机状态是否可以周期上报。
- 摄像机类型：USB、RTSP、网络相机或厂商 SDK。
- 是否需要算法盒子实时上报缺陷和截图。
